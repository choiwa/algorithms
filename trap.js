var trap = function (height) {
  let begin = height[0];
  let end;
  let count = 0;

  for (let i = 0; i < height.length; i += 1) {
    const curr = height[i];
    const next = height[i + 1];
  }

  return count;
};
